# ──────────────────────────────────────────────────────────────────────
# despy 환경 변수 예시 — 복사해서 .env.local 로 사용하세요.
# (.env.local 은 .gitignore 에 의해 커밋되지 않습니다)
# ──────────────────────────────────────────────────────────────────────

# MongoDB 연결 문자열 (shared/lib/db/mongodb.ts 에서 사용)
MONGODB_URI="mongodb://localhost:27017/despy"

# LLM API (AI 에이전트 계층 — 모델/토큰/프롬프트 정책 적용)
# LLM_API_KEY=""

# Judge0 (코드 채점 엔진 — Docker 배포 후 엔드포인트 지정)
# JUDGE0_URL="http://localhost:2358"
# JUDGE0_AUTH_TOKEN=""
