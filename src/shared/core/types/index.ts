/**
 * index.ts — 공유 타입 단일 출처 (배럴)
 *
 * 여러 레이어에서 공유되는 도메인 타입을 이곳에 단일 출처로 정의한다.
 * feature 전용 타입은 해당 feature 폴더 안에 두고, 공유되는 것만 승격한다.
 *
 * 사용처: features/*, shared/* 전반 (`@/shared/core/types`)
 */

// 도메인 공유 타입을 여기에 추가한다. (예: User, Exam, Problem, Submission)
export {};
